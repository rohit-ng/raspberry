#!/bin/sh
set -e
# Automatically added by dh_installinit/13.2~bpo10+1
if [ -x "/etc/init.d/pigpiod" ] && [ "$1" = remove ]; then
	invoke-rc.d pigpiod stop || exit 1
fi
# End automatically added section
